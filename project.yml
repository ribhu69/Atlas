name: AtlasFiles

options:
  bundleIdPrefix: com.ribhu69
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"
  createIntermediateGroups: true
  groupSortPosition: top
  tabWidth: 4
  indentWidth: 4

settings:
  base:
    SWIFT_VERSION: "6.0"
    ENABLE_PREVIEWS: true
    ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: true

targets:
  AtlasFiles:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Sources/Atlas
        excludes:
          - "**/.DS_Store"

    info:
      path: AtlasFiles/Info.plist
      properties:
        CFBundleDisplayName: Atlas Files
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        LSRequiresIPhoneOS: true
        UILaunchScreen:
          UIColorName: ""
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: true
          UISceneConfigurations: {}
        UIRequiredDeviceCapabilities:
          - arm64
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        LSSupportsOpeningDocumentsInPlace: true
        UIFileSharingEnabled: true
        NSUbiquitousContainers:
          iCloud.com.ribhu69.atlasfiles:
            NSUbiquitousContainerIsDocumentScopePublic: true
            NSUbiquitousContainerName: Atlas Files
            NSUbiquitousContainerSupportedFolderLevels: Any
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - com.atlas.files
            CFBundleURLName: com.ribhu69.atlasfiles
        NSPhotoLibraryUsageDescription: Used to import photos into Atlas Files
        NSCameraUsageDescription: Used to capture photos to save to your files
        NSLocalNetworkUsageDescription: Used to connect to local FTP and WebDAV servers
        ITSAppUsesNonExemptEncryption: false

    # No top-level entitlements key — each config points to its own file below

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ribhu69.atlasfiles
        PRODUCT_NAME: AtlasFiles
        INFOPLIST_FILE: AtlasFiles/Info.plist
        DEVELOPMENT_TEAM: ""
        SWIFT_EMIT_LOC_STRINGS: true
        ENABLE_BITCODE: false
        TARGETED_DEVICE_FAMILY: "1,2"
      configs:
        Debug:
          # No iCloud entitlement — free-tier dev account compatible
          CODE_SIGN_ENTITLEMENTS: AtlasFiles/AtlasFiles.Debug.entitlements
          DEBUG_INFORMATION_FORMAT: dwarf
          SWIFT_OPTIMIZATION_LEVEL: "-Onone"
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
        Release:
          # Full iCloud entitlement for App Store distribution
          CODE_SIGN_ENTITLEMENTS: AtlasFiles/AtlasFiles.Release.entitlements
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
          SWIFT_OPTIMIZATION_LEVEL: "-O"
          VALIDATE_PRODUCT: true
